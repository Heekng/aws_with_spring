spring:
  config:
    activate:
      on-profile: real2
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
        storage_engine: innodb
  session:
    store-type: jdbc
server:
  port: 8082