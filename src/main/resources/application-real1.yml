spring:
  config:
    activate:
      on-profile: real1
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
        storage_engine: innodb
  session:
    store-type: jdbc
server:
  port: 8081